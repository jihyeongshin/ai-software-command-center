# Export Manifest

Result: `RELEASE_ROLLED_BACK_TO_REPLAY_ONLY`

The archive contains the Task, executor evidence, final frontend bytes, affected test bytes, and the exact 1919 governance provenance. SHA-256 inventory is generated after assembly and recorded here before export.
